name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 10  # Prevent hung jobs
    strategy:
      fail-fast: false  # Continue testing other configs if one fails
      matrix:
        # Test 8 core configurations (2 samplers × 2 targets × 2 datasets)
        config:
          - { name: "DDIM/epsilon/MNIST", sampler: "ddim", target: "epsilon", dataset: "mnist", steps: 100 }
          - { name: "DDIM/v/MNIST", sampler: "ddim", target: "v", dataset: "mnist", steps: 100 }
          - { name: "DDIM/epsilon/CIFAR10", sampler: "ddim", target: "epsilon", dataset: "cifar10", steps: 100 }
          - { name: "DDIM/v/CIFAR10", sampler: "ddim", target: "v", dataset: "cifar10", steps: 100 }
          - { name: "EDM/epsilon/Euler/MNIST", sampler: "edm", target: "epsilon", solver: "euler", sample_steps: 10, dataset: "mnist" }
          - { name: "EDM/v/Heun/MNIST", sampler: "edm", target: "v", solver: "heun", sample_steps: 10, dataset: "mnist" }
          - { name: "EDM/epsilon/Euler/CIFAR10", sampler: "edm", target: "epsilon", solver: "euler", sample_steps: 10, dataset: "cifar10" }
          - { name: "EDM/v/Heun/CIFAR10", sampler: "edm", target: "v", solver: "heun", sample_steps: 10, dataset: "cifar10" }
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'  # Speed up dependency installs
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: Smoke test - ${{ matrix.config.name }}
        run: |
          # Build command dynamically based on config
          CMD="python nanoddpm-pro.py"
          CMD+=" --sampler ${{ matrix.config.sampler }}"
          CMD+=" --target ${{ matrix.config.target }}"
          CMD+=" --dataset ${{ matrix.config.dataset }}"
          CMD+=" --epochs 1"
          CMD+=" --batch_size 16"
          CMD+=" --resize 16"
          CMD+=" --device cpu"
          CMD+=" --cfg_scale 4.0"
          
          # Add sampler-specific flags
          if [ "${{ matrix.config.sampler }}" = "ddim" ]; then
            CMD+=" --steps ${{ matrix.config.steps }}"
          else
            CMD+=" --solver ${{ matrix.config.solver }}"
            CMD+=" --sample_steps ${{ matrix.config.sample_steps }}"
          fi
          
          echo "Running: $CMD"
          $CMD
      
      - name: Verify metrics output
        run: |
          if [ -f nanoddpm_pro_metrics.json ]; then
            echo "Metrics file generated successfully !!!"
            python -c "import json; d=json.load(open('nanoddpm_pro_metrics.json')); print(f'Epochs logged: {len(d)}')"
          else
            echo "Metrics file not found !!!"
            exit 1
          fi